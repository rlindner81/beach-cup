import store from "../store.ts";

const isLoggedIn = async (session) => {
};
const login = async (session, body) => {
};
const logout = async (session) => {
};
const getMe = async (session) => {
};

export { isLoggedIn, login, logout, getMe };
